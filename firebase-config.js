/* ============================================================
   STEP 1 — paste your Firebase project settings below.
   How to get these (5 minutes, free):
     1. Go to https://console.firebase.google.com  ->  Add project
     2. In the project: Build -> Firestore Database -> Create database
        (choose "Start in production mode", pick a location)
     3. Build -> Authentication -> Get started -> enable "Anonymous"
     4. Project settings (gear icon) -> scroll to "Your apps" ->
        tap the </> (Web) icon -> register app -> copy the config values
     5. Paste them below, replacing every PASTE_... value.
   Then set the security rules — see README.txt STEP 2.
   ============================================================ */

window.FIREBASE_CONFIG = {
  apiKey:            "PASTE_API_KEY",
  authDomain:        "PASTE_PROJECT.firebaseapp.com",
  projectId:         "PASTE_PROJECT_ID",
  storageBucket:     "PASTE_PROJECT.appspot.com",
  messagingSenderId: "PASTE_SENDER_ID",
  appId:             "PASTE_APP_ID"
};
